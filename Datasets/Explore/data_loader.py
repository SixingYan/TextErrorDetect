
def buildformat_fasttext(data_space, labels):
    """ data_space 输入的句子一定是已经用空格分开好的 """
    return '__{0}__ {1}'.format(labels[i], d) for i, d in enumerate(data_space)
    '''
    lines = []
    with open(path, 'w+') as f:
        f.writerows(lines)
    return len(data_space)
    '''


def get_kenlm()->Tuple:

    return data, labels


def get_paopao()->Tuple:
    """
        data, labels
    """

    return data, labels


